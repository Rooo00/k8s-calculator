apiVersion: v1
kind: Service
metadata:
  name: calculator-service
spec:
  type: NodePort
  selector:
    app: k8s-calculator
  ports:
    - protocol: TCP
      port: 80          
      targetPort: 5000  
      nodePort: 30080   